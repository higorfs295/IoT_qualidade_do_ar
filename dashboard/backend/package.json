{
  "name": "qar-ingestao",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ingestao local persistente: MQTT, contrato v1.1, REST, WebSocket, healthcheck e metricas.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "mqtt": "^5.10.1",
    "ws": "^8.18.0"
  }
}
